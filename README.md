# Angular Workflow with Webpack!!

A angular workflow created with webpack for more flexibility of user.

### Install :

```
  git clone 'https://github.com/dromestrome/angular-webpack.git'
  cd angular-webpack
  npm install
  npm start

```
